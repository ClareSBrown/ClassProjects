A simple shell created in my operating systems class. 
Has built in functions for exiting, cd, and status. Redirects
input and output accordingly.

To compile the program, use the following command:

gcc command.c smallshell.c main.c -o smallshell

To run the program, use the following command:

smallshell

